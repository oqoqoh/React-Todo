import React from 'react';
import TextSize from './components/TextSize';

const AppText = () => {
    return (
        <div>
            <TextSize />
        </div>
    );
};

export default AppText;
