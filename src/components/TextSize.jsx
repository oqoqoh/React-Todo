import React from 'react';

const TextSize = () => {
    return (
        <div
            style={{
                width: '100%',
                height: '100vh',
            }}>
            <div>텍스트 크기는 ? : {}</div>
            <div></div>
        </div>
    );
};

export default TextSize;
